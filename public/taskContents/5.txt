let result={}
    
    data.replace(/[^\w\s]/g, "").split(/\s+/).forEach( word => {
        if(result[word]){
            result[word]=result[word]+1
        }
        else{
            result[word]=1;
        }
    });
    return result;